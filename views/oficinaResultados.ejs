<!DOCTYPE html>
<html>

<%- include('./head.ejs') %>

    <body style="display: flex; justify-content: center;align-items: center; flex-direction: column;">
        <h1>
            <%= title %>
        </h1>
        <ul>
            <% oficinas.forEach(element => { %>
                <li type="none">
                    <%= element.id %> - <%= element.denominacion  %>
                </li>
                <% }) %>
        </ul>
    </body>