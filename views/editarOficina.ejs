<!DOCTYPE html>
<html>

<%- include('./head.ejs') %>

    <body>
        <h1>Editar datos de oficina</h1>
        <form action="/updateOficina/<%= item.id %>" method="post">
            <input type="text" name="denominacion" value="<%= item.denominacion %>" required>
           
            <!-- Nuevo campo -->
            <button type="submit">Update</button>
        </form>
    </body>

</html>